<h1>Tasks</h1>

<h2><%= @task.title %></h2>
<h4><%= @task.details %></h4>
<p><%= "The task is done" if @task.completed %></p>
<p><%= link_to "Edit", edit_task_path(@task) %></p>
<p><%= link_to "Delete", task_path(@task), method: :delete %></p>
